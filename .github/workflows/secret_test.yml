name: Secret Test

# Trigger manually
on:
  workflow_dispatch:

jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show secret safely
        run: echo "The secret is ${{ secrets.MY_SENSITIVE_TOKEN }}"
